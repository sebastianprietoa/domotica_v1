# Legacy examples

Estos archivos se conservaron solo como referencia historica del repo base de Tuya.

- fueron movidos fuera del flujo principal
- se eliminaron secretos hardcodeados
- no deben usarse como base de produccion

Para el flujo actual usa:

- `.env`
- `config/config.yaml`
- `scripts/`
- `src/ambilight_tuya/`
